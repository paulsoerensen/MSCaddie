@using System.Linq;
@using MSCaddie.Shared.Services
@page "/MatchPlay"
@inherits MatchPlayViewBase


@rendermode InteractiveServer
<h1>Hulspil</h1>

@if (players is not null)
{
    <div class="container">
        <div class="row py-5">
            <RadzenDataGrid @ref="playerGrid" AllowFiltering="false" FilterMode=FilterMode.Simple AllowPaging="false" AllowSorting="true"
                            Data="@teams" TItem="PlayerForMatchPlayModel" AllowVirtualization="true" AllowColumnResize="true">

                <Columns>
                    <RadzenDataGridColumn TItem="PlayerForMatchPlayModel" Property="Fullname" Title="Spiller" Width="12rem"/>
                    <RadzenDataGridColumn TItem="PlayerForMatchPlayModel" Property="League" Title="Single" Width="20rem" TextAlign="TextAlign.Center">
                        <EditTemplate Context="data">
                            <RadzenRadioButtonList @bind-Value=data.LeagueId TValue="int">
                                <Items>
                                    <RadzenRadioButtonListItem Text="A" Value="1" TValue="int" />
                                    <RadzenRadioButtonListItem Text="B" Value="2" TValue="int" />
                                    <RadzenRadioButtonListItem Text="Deltager ikke" Value="0" TValue="int" />
                                </Items>
                            </RadzenRadioButtonList>
                        </EditTemplate>
                    </RadzenDataGridColumn>
                    <RadzenDataGridColumn TItem="PlayerForMatchPlayModel" Property="Fullname2" Title="Makker" Width="20rem" >
                        <EditTemplate Context="data">
                            <RadzenDropDown @bind-Value="data.VgcNoPartner" Data="@players" 
                                TextProperty="@nameof(PlayerModel.Fullname)" 
                                ValueProperty="@nameof(PlayerModel.VgcNo)" 
                                Style="width:100%; display: block;"
                                InputAttributes="@(new Dictionary<string,object>(){ { "aria-label", "Vælg makker" }})" />
                        </EditTemplate>
                    </RadzenDataGridColumn>
                    <RadzenDataGridColumn Context="data" Filterable="false" Sortable="false" TextAlign="TextAlign.Right" FrozenPosition="FrozenColumnPosition.Right">
                        <Template Context="data">
@*                             <RadzenButton Icon="add_circle" ButtonStyle="ButtonStyle.Success" Variant="Variant.Flat" Size="ButtonSize.Medium" Shade="Shade.Lighter" Click="@(() => InsertAfterRow(data))" title="Add new row after this row" />
 *@                            <RadzenButton Icon="edit" ButtonStyle="ButtonStyle.Light" Variant="Variant.Flat" Size="ButtonSize.Medium" class="rz-my-1 rz-ms-1" Click="@(args => EditRow(data))" @onclick:stopPropagation="true" />
                            <RadzenButton Icon="delete" ButtonStyle="ButtonStyle.Danger" Variant="Variant.Flat" Size="ButtonSize.Medium" Shade="Shade.Lighter" class="rz-my-1 rz-ms-1" Click="@(args => DeleteRow(data))" @onclick:stopPropagation="true" />
                        </Template>
                        <EditTemplate Context="data">
                            <RadzenButton Icon="check" ButtonStyle="ButtonStyle.Success" Variant="Variant.Flat" Size="ButtonSize.Medium" Click="@((args) => SaveRow(data))" aria-label="Save" />
                            <RadzenButton Icon="close" ButtonStyle="ButtonStyle.Light" Variant="Variant.Flat" Size="ButtonSize.Medium" class="rz-my-1 rz-ms-1" Click="@((args) => CancelEdit(data))" aria-label="Cancel" />
                            <RadzenButton Icon="delete" ButtonStyle="ButtonStyle.Danger" Variant="Variant.Flat" Size="ButtonSize.Medium" Shade="Shade.Lighter" class="rz-my-1 rz-ms-1" Click="@(args => DeleteRow(data))" aria-label="Delete" />
                        </EditTemplate>
                    </RadzenDataGridColumn>
               
                </Columns>
            </RadzenDataGrid>
        </div>
    </div>



@*     <DataGrid TItems="Player" Data="@matches" Responsive>
        <DataGridColumn Field="@nameof(PlayerModel.SerieName)" Caption="Række"/>
        <DataGridNumericColumn Field="@nameof(Player.MatchDate)" Caption="Dato">
                <DisplayTemplate>
                    @{
                    var date = (context as Dto.MatchPlayer)?.MatchDate;

                        if (date != null)
                        {
                            @($"{date.Value.ToString("dddd dd MMM, yyyy")}")
                        }
                    }
                </DisplayTemplate>
        </DataGridNumericColumn>
        <DataGridColumn Field="@nameof(Dto.MatchPlayer.Course)" Caption="Bane" />
        <DataGridColumn Field="@nameof(Dto.MatchPlayer.Opponent)" Caption="Modstander" />
        <DataGridNumericColumn Field="@nameof(Dto.MatchPlayer.LastUpdate)" Caption="Opdateret">
            <DisplayTemplate>
                @{
                    var date = (context as Dto.MatchPlayer)?.LastUpdate;

                    if (date != null)
                    {
                        @($"{date.Value.ToString("dddd dd MMM, yyyy")}")
                    }
                }
            </DisplayTemplate>
        </DataGridNumericColumn>
        <DataGridCheckColumn Field="@nameof(Dto.MatchPlayer.Confirmed)" 
            TextAlignment="TextAlignment.Center" Caption="Bekræftet" />
    </DataGrid> 
 *@}



